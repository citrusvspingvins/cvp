<%= form_for(@nescafe) do |f| %>
  <% if @nescafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nescafe.errors.count, "error") %> prohibited this nescafe from being saved:</h2>

      <ul>
      <% @nescafe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
